/**
 *  This component act as the container for the content passed as the props.children.
 * @param {Object} props It contains the properties of the component.
 *
 */
function Wrapper(props) {
  return props.children;
}

export default Wrapper;
